{
  "name": "@edge-sync/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "edge-sync CLI: config editing, manual sync trigger, export/restore (IPC lands in stage 3)",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "start": "tsx src/main.ts"
  },
  "dependencies": {
    "@edge-sync/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
