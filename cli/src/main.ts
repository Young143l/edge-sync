import { PROTOCOL_VERSION } from '@edge-sync/protocol'

const VERSION = '0.1.0'

console.log(`edge-sync cli v${VERSION} (plugin protocol v${PROTOCOL_VERSION})`)
console.log('stage 0 scaffold — commands land in stage 3')
