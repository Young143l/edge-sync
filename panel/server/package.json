{
  "name": "@edge-sync/panel-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "edge-sync web panel server: REST API over kernel IPC + static hosting + download streams",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "start": "tsx src/main.ts"
  },
  "dependencies": {
    "@edge-sync/protocol": "workspace:*",
    "@hono/node-server": "^1.13.0",
    "archiver": "^7.0.1",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@types/archiver": "^6.0.3",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
